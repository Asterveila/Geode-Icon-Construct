{
	"geode": "5.10.1",
	"gd": {
		"win": "2.2081",
		"android": "2.2081",
		"mac": "2.2081",
		"ios": "2.2081"
	},
	"resources": {
		"sprites": [
			"resources/*.png"
		]
	},
	"settings": {
		"update-offsets-live": {
			"type": "bool",
			"name": "Update On Text Change",
			"description": "Attempts to update the Sprite Offsets anytime the Text Inputs are changed. \nToggleable in case this causes instability issues for anyone. Most notable issue is messing up the Touch Priority of the mod's popup. Installing the 'Better Touch Prio' mod is recommended in case you wanna keep this feature on.",
			"default": true
		},
		"renders-path": {
			"type": "folder",
			"name": "Renders Folder",
			"description": "Where to save the Icon Renders to. Defaults to this mod's config directory.",
			"default": "{mod_config_dir}/renders"
		},
		"bg-color": {
			"type": "rgba",
			"name": "Render Background Color",
			"description": "Defines the BG color to use for the renders. Defaults to transparent (which is best, honestly)",
			"default": [0, 0, 0, 0]
		},
		"extra-swing-space": {
			"type": "bool",
			"name": "Render - Extra Swing Space",
			"description": "Adds extra space to Swing Renders to make sure the Swing Fires fit inside the render, and aren't cut off.",
			"default": false
		},
		"animate-swing-fires": {
			"type": "bool",
			"name": "Animate Swing Fires",
			"description": "Plays the in-game looping Fire Animation on the preview's Swing Fires",
			"default": true
		},
		"trail-outer-border": {
			"type": "bool",
			"name": "Show Trail Outer Edges",
			"description": "For Basic Trail Previewing (non-wave). If enabled, uses a texture that adds 2 lines to mark the outer edges of the trail's boundary. These lines loosely indicate the size of bigger trails, such as the Spike trail or the Fire trail.",
			"default": true
		},
		"trail-blending": {
			"type": "bool",
			"name": "Trail Blending",
			"description": "For Basic Trail Previewing (non-wave). If enabled, the trail preview will be blended with the background. If disabled, the trail will be fully opaque.",
			"default": true
		},
		"color-trail": {
			"type": "bool",
			"name": "Color Trail Preview",
			"description": "For Basic Trail Previewing (non-wave). If enabled, the trail preview will be colored with the secondary color of the icon. If disabled, the trail will be white.",
			"default": true
		},
		"color-wave-trail": {
			"type": "bool",
			"name": "Color Wave Trail Preview",
			"description": "For Wave Trail Previewing. If enabled, the trail preview will be colored with the primary color of the icon. If disabled, the trail will be white.",
			"default": true
		},
		"draw-hitbox-fill": {
			"type": "bool",
			"name": "Draw Hitbox Fill",
			"description": "Enables drawing the hitbox fill in the Workbench Hitbox preview. \n\nIn case you're wondering why you can toggle this and not the border, it's because the border is mainly controlled via the slider in the popup.",
			"default": true
		}
	},
	"id": "saritahhh.offset_assist",
	"name": "Icon Construct",
	"version": "v1.5.0",
	"developer": "Saritahhh",
	"description": "All-in-one Utils for Icon Creators!",
	"tags": ["interface", "utility"],
	"links": {
		"source": "https://github.com/Asterveila/Geode-Icon-Construct"
	},
	"dependencies": {
		"geode.node-ids": ">=v1.20.0",
		"hiimjustin000.more_icons": {
            "version": ">=v2.0.0",
            "required": true
        }
	}
}
